@if (Request.IsAuthenticated) {
    <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
        <i class="icon-user"></i> @User.Identity.Name
        <span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
        <li><a href="#">Profile</a></li>
        <li class="divider"></li>
        <li><a href="#">Sign Out</a></li>
    </ul>
} else {
    <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
        <i class="icon-user"></i> Log In
        <span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
        <li><a href="@ViewBag.GoogleLoginUrl" id="googleLoginLink">Log in with Google</a></li>
        <li><a href="@ViewBag.FacebookLoginUrl" id="facebookLoginLink">Log in with Facebook</a></li>
        <li><a href="@ViewBag.TwitterLoginUrl" id="twitterLoginLink">Log in with Twitter</a></li>
        <li class="divider"></li>
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
    </ul>
}